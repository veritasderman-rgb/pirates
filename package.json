{
  "name": "pirates",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "vo:extract": "node scripts/extract-lines.mjs",
    "vo:stats": "node scripts/extract-lines.mjs --stats",
    "vo:gen": "node scripts/gen-voiceovers.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "^0.160.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "three": "^0.160.0"
  }
}
